# 功能更新日志 v1.1

## 🎉 新增功能（2026-08-20 15:45）

### ✨ 点击显示计算详情

**位置**：按模型统计表格

**功能**：点击任意模型行，展开显示该模型的详细计算逻辑

#### 详情面板包含

**1. Token 统计**
- 输入 Token（实际数字）
- 输出 Token（实际数字）
- 推理 Token（实际数字）
- 缓存读 Token（实际数字）
- 缓存写 Token（实际数字）
- 总计 Token（高亮显示）

**2. 缓存命中率计算**
- 显示公式：`cache_hit_rate = cache_read / (input + cache_read + cache_write)`
- 分步计算：
  - 分子（缓存读）
  - 分母（总计）
  - 命中率（高亮显示，精确到小数点后 2 位）

**3. 费用计算**
- 标准费用公式：
  ```
  = input × price_in + output × price_out
  + cache_read × price_cache_r + cache_write × price_cache_w
  + reasoning × price_out
  ```
- 标准费用结果（USD，6 位小数）
- 实际费用公式：`actual CNY = standard USD × multiplier / 12`
- 实际费用结果（CNY，6 位小数，高亮显示）

#### 交互方式

1. **点击展开**：点击任意模型行，展开详情面板
2. **自动切换**：点击另一个模型，自动关闭之前的详情
3. **手动关闭**：点击「关闭」按钮收起详情
4. **再次点击**：再次点击同一模型行，收起详情

#### UI 设计

- **布局**：三列网格布局，响应式设计
- **配色**：深色背景 + 蓝色边框 + 绿色数值
- **样式**：
  - 公式以代码样式显示
  - 关键结果高亮显示（蓝色背景）
  - 悬停效果（模型行变色）
  - 平滑过渡动画

#### 技术实现

**前端 JavaScript**：
- 动态生成详情行（隐藏的 `<tr>`）
- 点击事件监听器
- 自动关闭其他详情
- ID 生成（替换特殊字符）

**CSS 样式**：
- `.model-row` - 可点击行样式
- `.detail-cell` - 详情单元格
- `.detail-content` - 详情内容区
- `.detail-grid` - 三列网格
- `.detail-section` - 各部分卡片
- `.formula` - 公式显示框
- `.calculation` - 计算步骤
- `.calc-step.highlight` - 高亮结果

---

## 📊 使用场景

### 场景 1：理解费用计算
点击费用较高的模型 → 查看详细计算 → 了解哪部分 Token 贡献了费用

### 场景 2：优化缓存策略
点击缓存命中率低的模型 → 查看分步计算 → 分析原因

### 场景 3：验证数据准确性
点击任意模型 → 查看所有 Token 类型的数值 → 手动验证总计

---

## ✅ 测试方法

1. 启动 Dashboard：`python dashboard.py`
2. 访问：http://127.0.0.1:8899
3. 滚动到「按模型统计」表格
4. 点击任意模型行
5. 验证详情面板正确展开
6. 验证数据准确性
7. 验证交互行为（切换、关闭）

---

## 🎨 视觉效果

```
┌─ 模型行（可点击）─────────────────────────────────┐
│ gpt-5.6-sol | openai_2x | 6会话 | 14.81M | ...  │ ← 点击
└───────────────────────────────────────────────────┘
                     ↓
┌─ 详情面板 ─────────────────────────────────────────┐
│ 💡 gpt-5.6-sol 计算详情                            │
│                                                     │
│ ┌─ Token 统计 ─┐ ┌─ 缓存命中率 ─┐ ┌─ 费用计算 ─┐  │
│ │ 输入: 14.2M │ │ 公式:        │ │ 标准费用:  │  │
│ │ 输出: 580K  │ │ cache_read   │ │ $91.23     │  │
│ │ 推理: 225K  │ │ ─────────    │ │            │  │
│ │ 缓存读:74M  │ │ input+...    │ │ 实际费用:  │  │
│ │ 缓存写:2M   │ │              │ │ ¥16.73     │  │
│ │ ━━━━━━━━━   │ │ = 81.98%     │ │            │  │
│ │ 总计: 15M   │ │              │ │            │  │
│ └─────────────┘ └──────────────┘ └────────────┘  │
│                                       [关闭]       │
└───────────────────────────────────────────────────┘
```

---

## 📝 代码统计

- 新增 HTML：~120 行（详情模板）
- 新增 JavaScript：~25 行（点击处理）
- 新增 CSS：~160 行（样式定义）
- 总计：~305 行代码

---

## 🚀 后续可扩展

1. **导出详情**：添加「导出为 JSON」按钮
2. **对比模式**：同时展开多个模型进行对比
3. **历史趋势**：点击显示该模型的历史使用趋势图
4. **价格明细**：显示每种 Token 的单价和小计
5. **会话列表**：点击显示使用该模型的所有会话

---

更新完成！✨
